This repo holds the code used by GOV.UK to partition and flatten GA4 data using DataForm in the GCP project `gds-bq-reporting`


## Contributing

When making changes to the pipeline, unless specifically changing variables in the workflow settings files you should not commit them.

workflow_settings.aggregate_analysis.yaml

workflow_setting.dev.yaml

workflow_setting.prod.yaml

workflow_setting.yaml
